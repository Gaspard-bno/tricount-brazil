{
  "name": "tricount-brazil",
  "version": "1.0.0",
  "private": true,
  "description": "Comptes, listes et tâches partagés de Gaspard et Raphael au Brésil.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test tests/tricount-core.test.mjs tests/tricount-static.test.mjs",
    "lint": "eslint docs tests",
    "check": "npm test && npm run lint && node --check docs/app.js && node --check docs/core.js"
  },
  "devDependencies": {
    "eslint": "9.39.4"
  }
}
