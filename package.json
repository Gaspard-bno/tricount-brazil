{
  "name": "tricount-brazil",
  "version": "3.0.0",
  "private": true,
  "description": "Comptes, listes et tâches partagés de Gaspard et Raphael au Brésil.",
  "type": "module",
  "engines": {
    "node": ">=22.19"
  },
  "scripts": {
    "dev": "npm run icons && vite --host 127.0.0.1",
    "icons": "node scripts/build-icons.mjs",
    "build": "npm run icons && vite build && node scripts/finalize-build.mjs && npm run verify:build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/tricount-*.test.mjs",
    "lint": "eslint src scripts tests vite.config.ts",
    "check": "npm test && npm run lint && node --check src/app.js && node --check src/core.js",
    "verify:build": "node scripts/verify-build.mjs",
    "audit:performance": "lhci autorun --config=./lighthouserc.json"
  },
  "devDependencies": {
    "@lhci/cli": "0.15.1",
    "@phosphor-icons/core": "2.1.1",
    "axe-core": "4.13.0",
    "eslint": "9.39.4",
    "vite": "8.3.0"
  }
}
